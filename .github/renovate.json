{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>cgrindel/renovate_presets:ruleset_base",
    "github>cgrindel/renovate_presets:cgrindel_default"
  ],
  "postUpgradeTasks": {
    "commands": [
      "./do_renovate_post_upgrade"
    ],
    "executionMode": "branch"
  },
  "packageRules": [
    {
      "matchPackagePatterns": ["buildtools"],
      "matchManagers": ["gomod"],
      "enabled": false
    },
    {
      "description": "The fake registry used by swift_package_registry_example only hosts swift-collections 1.1.x, which is incompatible with swift-async-algorithms >= 1.1.4 (requires swift-collections >= 1.5.0).",
      "matchPackageNames": ["apple/swift-async-algorithms"],
      "matchFileNames": ["examples/swift_package_registry_example/Package.swift"],
      "enabled": false
    },
    {
      "description": "The BCR macOS Buildkite runner ships Swift 6.0.x and cannot parse swift-log's manifest from 1.9.0 onward (declares swift-tools-version >= 6.1). The bzlmod e2e test's workspace must stay pinned at swift-log < 1.9.0 so SPM resolves to 1.8.0 (tools 6.0). See #2262.",
      "matchPackageNames": ["apple/swift-log"],
      "matchFileNames": ["bzlmod/workspace/Package.swift"],
      "enabled": false
    },
    {
      "description": "aws-sdk-swift 1.7.0 and later attach smithy-swift's SmithyCodeGeneratorPlugin (a SwiftPM build tool plugin) to every service target. That plugin generates the per-operation schema code at build time, and this ruleset does not run build tool plugins yet, so the generated clients fail to compile under Bazel. aws_sdk_example must stay on 1.6.x until #46 is implemented.",
      "matchPackageNames": ["awslabs/aws-sdk-swift"],
      "matchFileNames": ["examples/aws_sdk_example/Package.swift"],
      "enabled": false
    }
  ]
}
