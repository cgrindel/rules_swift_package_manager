# Exclude this entire directory from having anything generated by Gazelle.  For
# the purposes of this example, the third_party, local, Swift packages should
# not have any BUILD.bazel files.  These packages are loaded using
# `local_swift_package`.
# gazelle:exclude **
