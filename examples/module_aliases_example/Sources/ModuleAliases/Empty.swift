// The ModuleAliases target exists only to declare SE-0339 module aliases in
// Package.swift. It is never built, but SwiftPM requires every target to have
// a source directory, so this placeholder file keeps `swift package describe`
// (used by rules_swift_package_manager) happy.
