# Exclude this entire directory from having anything generated by Gazelle. We
# want the local packages under this directory to be processed by
# local_swift_package not the Gazelle plugin.
# gazelle:exclude **
